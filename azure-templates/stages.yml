parameters:
  - name: stageName
    type: string
    default: Build

  - name: disableDiff
    type: boolean
    default: false

  # lvVersionsToBuild: list of {version, bitness}
  - name: lvVersionsToBuild
    type: object

  # codegenVis (optional): string array of codegen VI paths
  - name: codegenVis
    type: object
    default:
      - ''

  # dependencies (optional): list of {source, file, destination}
  - name: dependencies
    type: object
    default:
      - file: ''

  # buildSteps (optional): list of {projectLocation, buildOperation, target, buildSpec}
  - name: buildSteps
    type: object
    default: ''

  # packages (optional): list of {controlFileName, payloadMaps: list of {payloadLocation, installLocation} }
  - name: packages
    type: object
    default:
      - controlFileName: ''

  # submodules (optional): boolean of whether to use submodules
  - name: submodules
    type: boolean
    default: false

  # strings (releaseVersion, quarterlyReleaseVersion, buildOutputLocation, archiveLocation)
  - name: releaseVersion
    type: string
  - name: buildOutputLocation
    type: string
  - name: archiveLocation
    type: string

# Note: If custom build steps are needed for a particular custom device, copy this entire template into the azure-pipeline and add steps before/after as needed
stages:
  - stage: ${{ parameters.stageName }}_DiffVIs
    dependsOn: []
    condition: and(eq(variables['Build.Reason'], 'PullRequest'), eq('${{ parameters.disableDiff }}', false))
    pool:
      name: DevCentral-VeriStand-CustomDevices
      demands:
        - agent.os -equals Windows_NT
    jobs:
      - job: Find64bitVersion
        steps:
          - powershell: |
              $lvVersionsToBuild = '${{ convertToJson(parameters.lvVersionsToBuild) }}' | ConvertFrom-Json
              ForEach ($lvVersionToBuild in $lvVersionsToBuild)
              {
                if ($lvVersionToBuild.bitness -eq "64bit")
                {
                  $bitness = $lvVersionToBuild.bitness
                  $version = $lvVersionToBuild.version
                  Write-Host "##vso[task.setvariable variable=bitness;isOutput=true;]$bitness"
                  Write-Host "##vso[task.setvariable variable=version;isOutput=true;]$version"
                }
              }
            name: FindLast64bitVersion
      - job: DiffVIsforPR
        displayName: Diff VIs for PR
        dependsOn: ['Find64bitVersion']
        variables:
          - group: niveristand-custom-devices
          - name: diffBitness
            value: $[ dependencies.Find64bitVersion.outputs['FindLast64bitVersion.bitness'] ]
          - name: diffVersion
            value: $[ dependencies.Find64bitVersion.outputs['FindLast64bitVersion.version'] ]
        steps:
          - template: steps-prepare.yml
            parameters:
              build: False
              clearCache: False
          - template: steps-diff.yml
            parameters:
              lvVersionToDiff: $(diffVersion)

  - stage: ${{ parameters.stageName }}
    dependsOn: []
    variables:
      # Determine if any LabVIEW versions show up twice in the parameters list to avoid duplicate packaging
      ${{ each lvVersionToBuild in parameters.lvVersionsToBuild }}:
        ${{ each compareList in parameters.lvVersionsToBuild}}:
          ${{ if eq(compareList.version, lvVersionToBuild.version) }}: 
            ${{ if ne(compareList.bitness, lvVersionToBuild.bitness) }}:
              ${{ if eq(lvVersionToBuild.bitness, '32bit') }}:
                # if versions match, but bitnesses do not, mark as duplicate
                CD.DuplicateVersion.${{ lvVersionToBuild.version }}: True

      ${{ if ne(variables['Build.Reason'], 'PullRequest')}}:
        CD.BuildCounter: $[counter(format('custom {0}', variables['Build.SourceBranch']), 1)]
      ${{ if eq(variables['Build.Reason'], 'PullRequest')}}:
        CD.BuildCounter: "9999"
    pool:
      name: DevCentral-VeriStand-CustomDevices
      demands:
        - agent.os -equals Windows_NT
    jobs:
        # Build Jobs
      - ${{ each lvVersionToBuild in parameters.lvVersionsToBuild }}:
        - job: Job_Build_${{ lvVersionToBuild.version }}_${{ lvVersionToBuild.bitness }}
          displayName: Build LabVIEW ${{ lvVersionToBuild.version }} ${{ lvVersionToBuild.bitness }}
          timeoutInMinutes: 120
          steps:
            - template: steps-prepare.yml
              parameters:
                lvVersion:           ${{ lvVersionToBuild.version }}
                bitness:             ${{ lvVersionToBuild.bitness }}
                releaseVersion:      ${{ parameters.releaseVersion }}
                archiveLocation:     ${{ parameters.archiveLocation }}
                buildOutputLocation: ${{ parameters.buildOutputLocation }}
                codegenVis:          ${{ parameters.codegenVis }}
                submodules:          ${{ parameters.submodules }}

            - ${{ if ne( parameters.buildSteps, '' )}}:
              - ${{ each buildStep in parameters.buildSteps }}:
                  - template: steps-build.yml
                    parameters:
                      buildStep:        ${{ buildStep }}
                      dependencies:     ${{ parameters.dependencies }}
                      lvVersionToBuild: ${{ lvVersionToBuild }}

            - task: PowerShell@2
              displayName: Copy built files to Archive location
              inputs:
                targetType: 'filePath'
                filePath: 'niveristand-custom-device-build-tools/azure-templates/powershell-scripts/archive-build.ps1'
                failOnStdErr: True

        # Packaging Jobs
      - ${{ each lvVersionToBuild in parameters.lvVersionsToBuild }}:
        - job: Job_Package_${{ lvVersionToBuild.version }}_${{ lvVersionToBuild.bitness }}
          dependsOn:
            - ${{ each dependsLvVersionToBuild in parameters.lvVersionsToBuild }}:
              - ${{ if eq(dependsLvVersionToBuild.version, lvVersionToBuild.version) }}:
                - Job_Build_${{ dependsLvVersionToBuild.version }}_${{ dependsLvVersionToBuild.bitness }}
          displayName: Package LabVIEW ${{ lvVersionToBuild.version }} ${{ lvVersionToBuild.bitness }}
          condition: not(and(eq(variables['CD.DuplicateVersion.${{ lvVersionToBuild.version }}'], True),eq('${{ lvVersionToBuild.bitness }}', '32bit')))
          steps:
            - template: steps-prepare.yml
              parameters:
                  lvVersion:           ${{ lvVersionToBuild.version }}
                  bitness:             ${{ lvVersionToBuild.bitness }}
                  releaseVersion:      ${{ parameters.releaseVersion }}
                  archiveLocation:     ${{ parameters.archiveLocation }}
                  buildOutputLocation: ${{ parameters.buildOutputLocation }}
                  clearCache:          False

            - template: steps-packaging.yml
              parameters:
                packages:  ${{ parameters.packages }}

        # Finalization: if all jobs have passed, place .finished file in top level archive location
      - job: Finalize
        displayName: Final Validation
        dependsOn:
          - ${{ each lvVersionToBuild in parameters.lvVersionsToBuild }}:
            - Job_Build_${{ lvVersionToBuild.version }}_${{ lvVersionToBuild.bitness }}
            # only include packages that passed their job condition
            - ${{ if not(and(eq(variables['CD.DuplicateVersion.${{ lvVersionToBuild.version }}'], True),eq('${{ lvVersionToBuild.bitness }}', '32bit'))) }}:
              - Job_Package_${{ lvVersionToBuild.version }}_${{ lvVersionToBuild.bitness }}
        condition: succeeded()
        steps:
        - template: steps-prepare.yml # Configure variables, check out repos, Clear cache
          parameters:
            archiveLocation: ${{ parameters.archiveLocation }}
            build: False
            clearCache: False

        - task: PowerShell@2
          displayName: Add .finished to directory
          inputs:
            targetType: 'filePath'
            filePath: 'niveristand-custom-device-build-tools/azure-templates/powershell-scripts/finalization.ps1'
            failOnStdErr: True